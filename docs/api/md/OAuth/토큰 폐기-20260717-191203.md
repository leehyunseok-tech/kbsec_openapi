# API 명세서: 토큰 폐기-20260717-191203

## 기본 정보

| 항목 | 값 |
| --- | --- |
| API명 | 토큰폐기 |
| API인증 | 서비스 Key/토큰 |
| API보안 | SSL |
| 인터페이스 | REST (POST) |
| 데이터 | JSON |
| 버전 | v1.0 |
| 운영환경 | https://developer.kbsec.com:32484/oauth2/revoke |
| 최종작성일 | 2026-07-17 |

---

## INPUT (요청 파라미터)

| 항목영문명 | 항목한글명 | 타입(길이) | 필수여부 | 설명 |
| --- | --- | --- | --- | --- |
| token | 접근토큰 | String(440) | Y | OAuth 토큰
ex) eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzUxMiJ9.............. |
| clientId | 고객 앱키 | String(36) | Y | KB증권 홈페이지에서 발급받은 AppKey (절대 노출되지 않도록 주의해 주세요.) |
| clientSecret | 고객 앱Secret | String(32) | Y | KB증권 홈페이지에서 발급받은 AppSecret (절대 노출되지 않도록 주의해 주세요.) |

---

## OUTPUT (응답 데이터)

| 항목영문명 | 항목한글명 | 타입(길이) | 설명 |
| --- | --- | --- | --- |
| tokenRevoke | 토큰 폐기 성공 여부 | String(1) |  |

---

## AI 참고 사항

이 문서는 KB증권 OpenAPI 명세서입니다.

**요청 시 주의사항:**
- `dataHeader`에 `ipAddr`(IP주소)와 `macAddr`(MAC주소)를 포함해야 합니다.
- `dataBody`에 필수 파라미터를 모두 포함해야 합니다.

**응답 구조:**
- `dataHeader`: 결과 코드, 메시지, 처리 시간 등 메타 정보
- `dataBody`: 실제 응답 데이터 (배열인 경우 `out2` 등의 키로 포함)

**필드 타입 설명:**
- `C숫자`: 문자열 (숫자는 최대 길이)
- `N숫자`: 숫자
- `P숫자`: 소수점 포함 숫자 (숫자는 소수점 자릿수)
